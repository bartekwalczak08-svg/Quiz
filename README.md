# LLM Quiz Engine

Quiz engine powered by LLM with Pydantic models and Docker.

## Setup

```bash
pip install -r requirements.txt


# LLM Quiz Engine

Quiz engine powered by LLM with Pydantic models and Docker.

## Setup

```bash
# Instalacja zależności
pip install -r requirements.txt
pytest -v
 docker compose up --build
docker compose up -d --build
docker compose down

